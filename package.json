{
  "name": "hentai-reader",
  "private": true,
  "version": "3.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/image-load-coordinator.test.ts tests/net-limiter.test.ts",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.7.0",
    "vite": "^6.3.0",
    "vite-plugin-monkey": "^7.1.0"
  },
  "dependencies": {
    "photoswipe": "^5.4.4"
  }
}
